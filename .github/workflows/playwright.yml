name: Playwright Tests
on:
  push:
    branches: [ main ]
# git push
jobs:
  playwrighttests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/Checkout@v2
      - name: Say hello
        run: echo hello